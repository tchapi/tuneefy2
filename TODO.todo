API :

 ☐ Implémenter une limite pour l'API (temps / requetes par heure / nb total)

Dashboard :
  ☐ API Usage par client
    -chart :
SELECT  DATE(created_at) day, COUNT(id) count
FROM    items
WHERE client_id = 'administrator'
GROUP   BY  DATE(created_at)

 ☐  image de meilleure qualité pour chaque plateforme ?
